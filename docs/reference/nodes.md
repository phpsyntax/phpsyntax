# Nodes

Generated by `composer reference` from grammar/nodes.php and the node classes; do not edit. 119 node classes under `PhpSyntax\Nodes` and the 11 base classes and interfaces they build on. A slot type is `Token`, a node class, `?` when optional, `A|B` for a choice, `NodeList<T>` or `SeparatedNodeList<T>` for a list; the layout role is what the slot is to the indentation of the lines the node spreads over (`PhpSyntax\LayoutRole`). The members are the public properties and methods the class itself declares, so what every node answers stands under `Node` and what every expression answers under `ExpressionNode`.

## Base classes

### Node

Node of the concrete syntax tree; every token of the source is reachable through the children.

| Member | Description |
|---|---|
| `$parent: ?Node` | The node this one belongs to; only the tree writes it, through attachTo(). |
| `$text: string` | The node as it is written, without the trivia on its outer edges, which printing it writes too. |
| `$leadingTrivia: list&lt;Trivia>` | The trivia before the node, which are the leading trivia of its first token; setEdgeTrivia() writes them. |
| `$trailingTrivia: list&lt;Trivia>` | The trivia after the node, which are the trailing trivia of its last token; setEdgeTrivia() writes them. |
| `getChildren(): array` | Children in source order, without empty slots. |
| `replaceChild(Node\|Token $old, Node\|Token $new): void` | Replaces a direct child; the new one must fit the type of the slot. |
| `findSlotOf(Node\|Token $child): ?string` | The slot the direct child stands in; null when it is not a child. |
| `setSlot(string $slot, Node\|Token\|null $value): void` | Writes a slot: the value takes the place of the current one, which leaves the tree; the typed property refuses a value that does not fit the slot. The generated setters are the typed face of this method. |
| `getFile(): ?FileNode` |  |
| `getTokens(): array` | The tokens of the whole subtree in source order; empty for a node without tokens, such as an empty list. |
| `getTokenTexts(): array` | The texts of the tokens of the subtree in source order, the trivia left out: what matches() compares two nodes by, and what a tool keys a map by where the layout of the code must not count. The size of the code is measured on $text, which keeps the layout. |
| `getFirstToken(): ?Token` | Null only for a node without tokens, such as an empty list. |
| `getLastToken(): ?Token` |  |
| `getStartLine(): ?int` | Current line of the first token; null for a detached subtree or a node without tokens. |
| `getEndLine(): ?int` | Current line where the last token ends. |
| `getDocComment(): ?Trivia` | Doc comment before the node: the last one in the leading trivia of the first token, or in the trailing trivia of the previous token (public $a; /** @var int * / public $b;). |
| `replaceTrivia(Trivia $old, Trivia $new): void` | Replaces one trivia of the node, wherever among its tokens it stands, with another in place. |
| `removeTrivia(Trivia $trivia): void` | Removes one trivia of the node, wherever among its tokens it stands, tidying the whitespace around it the way Token::removeTrivia() does. |
| `replaceDocComment(Trivia $docComment): void` | Replaces the doc comment of the node (see getDocComment()) with the trivia given. |
| `removeDocComment(): void` | Removes the doc comment of the node (see getDocComment()) together with the line it stands on. |
| `findAncestor(string $class): ?Node` |  |
| `findFirst(string $class, ?callable $predicate = null): ?Node` | The first descendant of the class the predicate accepts, in pre-order; null when there is none. |
| `find(string $class, ?callable $predicate = null): array` | Descendant nodes of the class the predicate accepts, in pre-order, as a snapshot safe to iterate while mutating the tree. |
| `matches(Node $other): bool` | Whether the tokens of both nodes carry the same texts, whatever the whitespace between them. |
| `hasComment(): bool` | Whether a comment sits anywhere between the first and the last token of the node; the trivia on its outer edges do not count. |
| `getComments(): array` | The comments inside the node, in source order; those on its outer edges are not among them, the same way hasComment() does not count them. |
| `setEdgeTrivia(?array $leading = null, ?array $trailing = null): void` | Writes the trivia on the outer edges of the node: before its first token and after its last one. A null leaves that edge alone, [] clears it, and a node without tokens takes neither. |
| `replaceWith(Node $node): void` | Replaces this node in its parent; the trivia around the old node stay in place around the new one, and where the new one then stands right against a token it would be read together with, `.` against `1` or `return` against `FOO`, a space keeps the two apart. |
| `remove(CommentPolicy $comments = CommentPolicy::MoveToNextToken): void` | Removes this node from its list. A node alone on its lines takes the lines with it (indentation and line ending), otherwise the whitespace around stays; comments inside, each with the line ending that follows it, go where the policy says. |
| `__toString(): string` | The node printed back to source, the trivia on its outer edges included; $text leaves them out. |

### ClassLikeNode

Declaration with members: class, interface, trait, enum, anonymous class; an interface, because an anonymous class is an expression and the rest are statements.

| Member | Description |
|---|---|
| `$name: ?IdentifierNode` |  |
| `$members: NodeList` |  |

### ExpressionNode

Extends `Node`.

| Member | Description |
|---|---|
| `getAccessKind(): ?AccessKind` | How the parent reaches into this expression, null where it does not: `$this->x` and `$this[0]` read a member, `$this()` calls it, `$this::y()` takes it for the name of a class. |
| `isDereferenced(): bool` | Whether the parent reads a member, an element or a static member of this expression, or calls it: `$this->x`, `$this[0]`, `$this::y()`, `$this()`. Such an expression needs parentheses unless the grammar makes it a primary one. |
| `isDereferenceable(AccessKind $by = AccessKind::Member): bool` | Whether what is written after the expression may reach into it with no parentheses around it. A call takes a name or a member written before it for its own, which calls another thing entirely, and :: takes the name of a class, which a name written there would become. |
| `canNameClass(): bool` | Whether the expression may stand where a class is named: a variable and what is read out of one all the way down, a property or an element, since `new f()->b` instantiates f and `new A::B[0]` is no code. |
| `isWritable(): bool` | Whether the expression may stand where a place is assigned to: a variable, an element, a property, reached through a chain PHP writes through. A ?-> anywhere along it rules the write out, a call in the chain included, and so does a chain starting at a value of its own, a literal, a constant, `new` or `clone`, which has no place to write to. It says nothing about reading; that is isRepeatableRead(), and the two answer of different halves of `=`. |
| `isRepeatableRead(): bool` | Whether reading the expression again gives the same value with no side effects: variables, property, constant and offset fetches and literals, nothing that runs code of its own. The answer is syntactic, so what the language runs behind such a read is out of sight and does not count: a magic getter or a property hook behind a fetch, an ArrayAccess behind an offset, a __toString() behind a string that interpolates. Whoever cannot assume that much has to know the types, which a syntax tree does not. |
| `toValue(): mixed` | The value the expression is written as: a scalar, null, true, false, or an array of them. A name standing for a constant is not one, its value being a matter of what the code around it defines. |
| `hasValue(): bool` | Whether the expression is written as a value, which is what toValue() gives. |

### FunctionLikeNode

Declaration with parameters and a body: function, method, closure, arrow function, property hook; an interface, because a closure and an arrow function are expressions and the rest are not.

| Member | Description |
|---|---|
| `$parameters: ?SeparatedNodeList` |  |
| `$returnType: ?TypeNode` | The return type declared; null where none is, a property hook having none to declare. |

### MemberNode

Member of a class-like declaration: property, constant, method, trait use, enum case.

Extends `Node`.

### Member\TraitAdaptationNode

What a use statement says about a method it takes from a trait: an alias or a precedence. The trait is not declared here, an alias being able to leave it out where a precedence has to name it.

Extends `Node`.

| Member | Description |
|---|---|
| `$method: IdentifierNode` |  |
| `$semicolon: Token` |  |

### OperatorNode

Expression written with an operator, which is what decides whether parentheses around it are needed. An arrow function is one of them: its body reaches as far as the code lets it.

| Member | Description |
|---|---|
| `getPrecedence(): array` | How tightly the operator binds and which side it leans to; the higher the number the tighter, in the order the precedence declarations of the grammar put the operators. |

### RightExtendingNode

Operator written with no left operand, whose right one reaches as far as the code lets it: `fn() => …`, `throw`, `print`, `yield`, `yield from` and `include`. It can capture nothing before itself, so where nothing follows it, it stands bare however loosely it binds, and everywhere else it needs parentheses.

Implements `OperatorNode`.

### ScalarNode

Literal written in the source: a number, a string in any of its delimiters, an interpolated string, a boolean, null and a magic constant. What it stands for is `hasValue()` and `toValue()`, which an interpolating string and a magic constant answer no to, standing for what the code around them says.

Extends `ExpressionNode`.

### StatementNode

Extends `Node`.

### TypeNode

Extends `Node`.

| Member | Description |
|---|---|
| `allowsNull(): bool` | Whether the type accepts null: ?T, a union with null, and mixed. |

## Node classes

### NameNode

Name of a class, function, constant or namespace: one token of any kind, including keywords the grammar accepts as names (static, array, readonly).

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `token` | `Token` | Content |

| Member | Description |
|---|---|
| `$text: string` | The name as it is written, the leading backslash and the "namespace" prefix included. Writing it replaces the token with the one the name is written as, so that a qualified name does not stay an identifier; the trivia around it and its place in the original file stay with it. |
| `$kind: NameKind` |  |
| `$parts: list&lt;string>` | Segments of the name without the leading backslash or the "namespace" prefix. |
| `$shortName: string` | The last segment, which is what an import of the name brings in. |
| `$role: SymbolKind` | Which table of names the name belongs to: functions when called, constants when fetched, what a use item imports, otherwise classes, a namespace among them. It follows the place in the tree, so moving the node changes it, and it says nothing about whether the name refers to a symbol or declares one, which is isDeclaration(): whoever resolves names asks that first. |
| `static fromText(string $text): NameNode` | A name written as the text, in the token the name is written as (Foo, A\B, \A\B, namespace\B). |
| `isKeyword(): bool` | Whether the name is a keyword the grammar accepts in place of a name (static, array, readonly, exit...). |
| `isFullyQualified(): bool` |  |
| `isUnqualified(): bool` |  |
| `isSpecialClass(): bool` | Whether the name is self, static or parent, which stand for a class only where they are written. |
| `isDeclaration(): bool` | Whether the name declares or imports a symbol instead of referring to one: a namespace statement or a use. |
| `isReference(): bool` | Whether the name refers to a symbol of its table, one the resolver looks up: not the name a use or a namespace statement introduces, not self, static or parent where a class is named, which stand for one only where they are written, and not a builtin type where a type is written. A keyword read as a name refers where it stands for a symbol, as readonly(...) calls a function of that name, and so does self() or the constant parent. |
| `equals(string $name): bool` | Whether the name is written the same, letter case aside where PHP ignores it: a constant is compared exactly, a class, a function and a namespace are not. The leading backslash is part of the writing and so part of the comparison; whether two names mean the same class is what NameResolver answers. |

### IdentifierNode

Identifier of a declaration, member, label, hook, alias or named argument: one token of any kind, including reserved words.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `token` | `Token` | Content |

| Member | Description |
|---|---|
| `$text: string` | The identifier as it is written. Whether its letter case matters is up to what it names, so comparing it is left to the caller: a member and a label are case-sensitive, a magic method is not. Writing it takes an identifier and nothing else, so that no whitespace ends up in the text of a token. |
| `static fromText(string $text): IdentifierNode` | An identifier written as the text, which takes an identifier and nothing else. |

### ArgumentListNode

Parenthesized arguments of a call, instantiation, attribute or exit.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `openParen` | `Token` | Anchor |
| `items` | `SeparatedNodeList&lt;ArgumentNode\|VariadicPlaceholderNode\|ArgumentPlaceholderNode>` | Content |
| `closeParen` | `Token` | Closes |

| Member | Description |
|---|---|
| `isPartialApplication(): bool` | Whether the list leaves parameters unbound with ? or ..., which makes a closure of the call instead of calling it. |
| `findArgument(string $name, int $position): ?ArgumentNode` | The argument the parameter of the name and the position gets: the one written with the name, else the one standing at the position, so that get_class(object: $o) reads as the call get_class($o) is. Null where the parameter gets none, and where the call does not say which it gets: a ? placeholder holds a place without being an argument, and an unpacked array stands for as many arguments as it holds, so it takes the answer from a position after it, never from a name, which stands for itself. |

### ArgumentNode

Argument of a call: optionally named, by reference or unpacked.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `name` | `?IdentifierNode` | Content |
| `colon` | `?Token` | Content |
| `ampersand` | `?Token` | Content |
| `ellipsis` | `?Token` | Content |
| `value` | `ExpressionNode` | Content |

### VariadicPlaceholderNode

The ... placeholder of a first-class callable: f(...).

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `ellipsis` | `Token` | Content |

### ArgumentPlaceholderNode

The ? placeholder of a partial function application, optionally named: f(?), f(name: ?).

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `name` | `?IdentifierNode` | Content |
| `colon` | `?Token` | Content |
| `question` | `Token` | Content |

### ParameterNode

Parameter of a function, method, closure or hook; with modifiers it promotes a property.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `attributes` | `NodeList&lt;AttributeGroupNode>` | Anchor |
| `modifiers` | `ModifiersNode` | Anchor |
| `type` | `?TypeNode` | Anchor |
| `ampersand` | `?Token` | Anchor |
| `ellipsis` | `?Token` | Anchor |
| `variable` | `Expression\VariableNode` | Anchor |
| `equals` | `?Token` | Content |
| `default` | `?ExpressionNode` | Content |
| `openBrace` | `?Token` | Anchor |
| `hooks` | `?NodeList&lt;Member\PropertyHookNode>` | Content |
| `closeBrace` | `?Token` | Closes |

| Member | Description |
|---|---|
| `isPromoted(): bool` | Whether the parameter declares a property of the class, which its modifiers make it do. |

### AttributeGroupNode

One #[...] group of attributes.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `openAttribute` | `Token` | Anchor |
| `attributes` | `SeparatedNodeList&lt;AttributeNode>` | Content |
| `closeBracket` | `Token` | Closes |

### AttributeNode

Attribute with optional arguments.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `name` | `NameNode` | Content |
| `arguments` | `?ArgumentListNode` | Content |

### ModifiersNode

Modifier keywords in source order (public, static, readonly, abstract, final, var, public(set)...); may be empty.

Extends `Node`. Handwritten class.

| Slot | Type | Layout |
|---|---|---|
| `tokens` | `list&lt;Token>` | Anchor |

| Member | Description |
|---|---|
| `$visibility: ?Visibility` | The visibility the modifiers declare, null when they declare none: a member without one is public, a parameter without one is not promoted. |
| `$writeVisibility: ?Visibility` | The visibility for writing, which asymmetric visibility declares apart: public(set) and its kin. |
| `getTokens(): array` |  |
| `isPublic(): bool` | Whether the member is public, which it also is without a visibility of its own. |
| `isProtected(): bool` |  |
| `isPrivate(): bool` |  |
| `isStatic(): bool` |  |
| `isAbstract(): bool` |  |
| `isFinal(): bool` |  |
| `isReadonly(): bool` |  |
| `findToken(int $kind): ?Token` | The modifier token of the kind, null when the modifiers do not have it. |
| `isEmpty(): bool` |  |
| `has(int $kind): bool` |  |
| `append(Token $token): void` |  |
| `removeToken(Token $token): void` |  |
| `getChildren(): array` |  |
| `findSlotOf(Node\|Token $child): ?string` |  |
| `replaceChild(Node\|Token $old, Node\|Token $new): void` |  |
| `count(): int` |  |
| `getIterator(): ArrayIterator` | The modifier tokens, as a snapshot safe to iterate while mutating them. |

### NodeList

Sequence of nodes without separators: statements, members, attribute groups.

Extends `Node`. Handwritten class.

| Slot | Type | Layout |
|---|---|---|
| `items` | `list&lt;Node>` | Content |

| Member | Description |
|---|---|
| `getItems(): array` |  |
| `isEmpty(): bool` |  |
| `append(Node $item): void` | Appends an item, which takes the place in the lines of the list its neighbor has. |
| `insert(int $index, Node $item): void` | Inserts an item at the index. In a list standing in a file, an item that carries no trivia of its own takes the indentation of its neighbor and ends its line the same way. |
| `removeItem(Node $item): void` |  |
| `indexOf(Node $item): int` |  |
| `getChildren(): array` |  |
| `findSlotOf(Node\|Token $child): ?string` |  |
| `replaceChild(Node\|Token $old, Node\|Token $new): void` |  |
| `count(): int` |  |
| `getIterator(): ArrayIterator` | The items, as a snapshot safe to iterate while mutating the list. |

### SeparatedNodeList

Sequence of nodes with separator tokens between them and an optional trailing separator: parameters, arguments, array items, imports. An item may be an empty node standing for nothing between two separators ([, $b] = $x).

Extends `Node`. Handwritten class.

| Slot | Type | Layout |
|---|---|---|
| `items` | `list&lt;Node>` | Content |
| `separators` | `list&lt;Token>` | Content |

| Member | Description |
|---|---|
| `getItems(): array` |  |
| `getSeparators(): array` |  |
| `isEmpty(): bool` |  |
| `hasTrailingSeparator(): bool` |  |
| `append(Node $item, ?Token $separator = null): void` | Appends an item; the separator before it is derived from the existing ones unless given. |
| `insert(int $index, Node $item, ?Token $separator = null): void` | Inserts an item at the index. A missing separator is modeled on the existing ones, or on ", " in a one-line list; in a multi-line list the item also inherits the indentation of its neighbor. |
| `setTrailingSeparator(?Token $separator): void` |  |
| `removeItem(Node $item): void` | Removes the item together with the separator that goes with it (see findSeparatorOf()). |
| `findSeparatorOf(Node $item): ?Token` | The separator that goes when the item goes: the one after it, and for the last item the one before it. |
| `indexOf(Node $item): int` |  |
| `getChildren(): array` |  |
| `findSlotOf(Node\|Token $child): ?string` |  |
| `replaceChild(Node\|Token $old, Node\|Token $new): void` |  |
| `count(): int` |  |
| `getIterator(): ArrayIterator` | The items without the separators between them, as a snapshot safe to iterate while mutating the list. |

### FileNode

Root of the tree: the statements of a file and the end-of-file token carrying the trailing trivia.

Extends `Node`. Handwritten class.

| Slot | Type | Layout |
|---|---|---|
| `statements` | `NodeList&lt;StatementNode>` | Anchor |
| `endOfFile` | `Token` | Anchor |

| Member | Description |
|---|---|
| `$revision: int` | version of the tree: every write to a slot, a list, or the text or trivia of a token increments it |
| `getIndex(): TokenIndex` |  |

### AnonymousClassNode

Anonymous class in a new expression: new class(...) extends A implements B { ... }.

Extends `Node`. Implements `ClassLikeNode`.

| Slot | Type | Layout |
|---|---|---|
| `attributes` | `NodeList&lt;AttributeGroupNode>` | Anchor |
| `modifiers` | `ModifiersNode` | Anchor |
| `classKeyword` | `Token` | Anchor |
| `arguments` | `?ArgumentListNode` | Content |
| `extendsKeyword` | `?Token` | Content |
| `extends` | `?NameNode` | Content |
| `implementsKeyword` | `?Token` | Content |
| `implements` | `?SeparatedNodeList&lt;NameNode>` | Content |
| `openBrace` | `Token` | Anchor |
| `members` | `NodeList&lt;MemberNode>` | Content |
| `closeBrace` | `Token` | Closes |

| Member | Description |
|---|---|
| `$name: ?IdentifierNode` |  |

### ArrayItemNode

Item of an array literal or a destructuring list: value with optional key, by reference or unpacked.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `key` | `?ExpressionNode` | Content |
| `doubleArrow` | `?Token` | Content |
| `ampersand` | `?Token` | Content |
| `ellipsis` | `?Token` | Content |
| `value` | `ExpressionNode\|Expression\ListNode` | Content |

### EmptyArrayItemNode

Skipped item of a destructuring list ([, $b] = $x); has no tokens.

Extends `Node`.

### MatchArmNode

Arm of a match expression: the values compared with the subject, or default, and the result.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `values` | `?SeparatedNodeList&lt;ExpressionNode>` | Anchor |
| `defaultKeyword` | `?Token` | Anchor |
| `defaultComma` | `?Token` | Content |
| `doubleArrow` | `Token` | Content |
| `body` | `ExpressionNode` | Content |

### ClosureUsesNode

The use (...) clause of a closure.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `useKeyword` | `Token` | Anchor |
| `openParen` | `Token` | Anchor |
| `variables` | `SeparatedNodeList&lt;ClosureUseNode>` | Content |
| `closeParen` | `Token` | Closes |

### ClosureUseNode

Variable captured by a closure, optionally by reference.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `ampersand` | `?Token` | Content |
| `variable` | `Expression\VariableNode` | Content |

### ElseIfNode

elseif branch, in either syntax.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `elseifKeyword` | `Token` | Anchor |
| `openParen` | `Token` | Anchor |
| `condition` | `ExpressionNode` | Content |
| `closeParen` | `Token` | Closes |
| `body` | `?StatementNode` | Body |
| `colon` | `?Token` | Content |
| `statements` | `?NodeList&lt;StatementNode>` | Content |

### ElseNode

else branch, in either syntax; else if is an else with an if statement as the body.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `elseKeyword` | `Token` | Anchor |
| `body` | `?StatementNode` | Body |
| `colon` | `?Token` | Content |
| `statements` | `?NodeList&lt;StatementNode>` | Content |

### CaseNode

case or default of a switch; the separator is a colon or a semicolon.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `caseKeyword` | `Token` | Anchor |
| `value` | `?ExpressionNode` | Content |
| `separator` | `Token` | Content |
| `statements` | `NodeList&lt;StatementNode>` | Content |

### CatchNode

catch clause with one or more types and an optional variable.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `catchKeyword` | `Token` | Anchor |
| `openParen` | `Token` | Anchor |
| `types` | `SeparatedNodeList&lt;NameNode>` | Content |
| `variable` | `?Expression\VariableNode` | Content |
| `closeParen` | `Token` | Closes |
| `body` | `Statement\BlockNode` | Body |

### FinallyNode

finally clause.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `finallyKeyword` | `Token` | Anchor |
| `body` | `Statement\BlockNode` | Body |

### DeclareItemNode

Directive of a declare statement: strict_types=1.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `name` | `IdentifierNode` | Content |
| `equals` | `Token` | Content |
| `value` | `ExpressionNode` | Content |

### StaticVariableNode

Variable of a static statement with an optional initializer.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `variable` | `Expression\VariableNode` | Content |
| `equals` | `?Token` | Content |
| `default` | `?ExpressionNode` | Content |

### UseItemNode

Imported name with an optional alias; the type (function, const) appears only inside a group use. What the item imports is said by the statement it stands in, the prefix of a group included, so an item is not moved from one statement to another but written anew by UseNode::addImport().

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `type` | `?Token` | Content |
| `name` | `NameNode` | Content |
| `asKeyword` | `?Token` | Content |
| `alias` | `?IdentifierNode` | Content |

| Member | Description |
|---|---|
| `$kind: SymbolKind` | What the item imports: its own type where a group use writes one per item, else what the statement imports. |
| `$fullName: string` | The name the item imports, without a leading backslash: what is written here, and in a group use the prefix of the statement before it. |
| `getStatement(): ?Statement\UseNode` | The import the item belongs to; null for an item that is not in one. |

### ConstItemNode

Constant of a const statement or a class constant declaration.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `name` | `IdentifierNode` | Content |
| `equals` | `Token` | Content |
| `value` | `ExpressionNode` | Content |

### Expression\VariableNode

Variable: $a, $$a, ${expr}; inside a string also a bare name in ${name}.

Extends `ExpressionNode`.

| Slot | Type | Layout |
|---|---|---|
| `dollar` | `?Token` | Content |
| `openBrace` | `?Token` | Anchor |
| `name` | `Token\|ExpressionNode` | Content |
| `closeBrace` | `?Token` | Closes |

| Member | Description |
|---|---|
| `$plainName: ?string` | The name without the dollar sign; null where the name is an expression ($$a, ${expr}). |

### Expression\ArrayAccessNode

Array or string offset access: $a[$i], $a[].

Extends `ExpressionNode`.

| Slot | Type | Layout |
|---|---|---|
| `expression` | `ExpressionNode` | Content |
| `openBracket` | `Token` | Anchor |
| `index` | `?ExpressionNode` | Content |
| `closeBracket` | `Token` | Closes |

### Expression\PropertyFetchNode

Property access with -> or ?->; the name may be an identifier, a variable or a braced expression.

Extends `ExpressionNode`.

| Slot | Type | Layout |
|---|---|---|
| `object` | `ExpressionNode` | Content |
| `operator` | `Token` | Link |
| `openBrace` | `?Token` | Anchor |
| `name` | `IdentifierNode\|ExpressionNode` | Content |
| `closeBrace` | `?Token` | Closes |

| Member | Description |
|---|---|
| `isNullsafe(): bool` | Whether the fetch is written with ?->, which skips it when the object is null. |

### Expression\StaticPropertyFetchNode

Static property access: A::$b, A::$$b, A::${expr}. The name is written the way a variable is, dollar and braces included, but it names a property.

Extends `ExpressionNode`.

| Slot | Type | Layout |
|---|---|---|
| `class` | `NameNode\|ExpressionNode` | Content |
| `doubleColon` | `Token` | Content |
| `dollar` | `?Token` | Content |
| `openBrace` | `?Token` | Anchor |
| `name` | `Token\|ExpressionNode` | Content |
| `closeBrace` | `?Token` | Closes |

| Member | Description |
|---|---|
| `$plainName: ?string` | The name of the property without the dollar sign; null where the name is an expression ($$b, ${expr}). |

### Expression\ClassConstantFetchNode

Class constant access: A::B, A::class, A::{expr}.

Extends `ExpressionNode`.

| Slot | Type | Layout |
|---|---|---|
| `class` | `NameNode\|ExpressionNode` | Content |
| `doubleColon` | `Token` | Content |
| `openBrace` | `?Token` | Anchor |
| `name` | `IdentifierNode\|ExpressionNode` | Content |
| `closeBrace` | `?Token` | Closes |

### Expression\ConstantFetchNode

Constant access by name: FOO, \Foo\BAR.

Extends `ExpressionNode`.

| Slot | Type | Layout |
|---|---|---|
| `name` | `NameNode` | Content |

### Expression\FunctionCallNode

Function call by name or on an expression.

Extends `ExpressionNode`.

| Slot | Type | Layout |
|---|---|---|
| `name` | `NameNode\|ExpressionNode` | Content |
| `arguments` | `ArgumentListNode` | Content |

### Expression\MethodCallNode

Method call with -> or ?->.

Extends `ExpressionNode`.

| Slot | Type | Layout |
|---|---|---|
| `object` | `ExpressionNode` | Content |
| `operator` | `Token` | Link |
| `openBrace` | `?Token` | Anchor |
| `name` | `IdentifierNode\|ExpressionNode` | Content |
| `closeBrace` | `?Token` | Closes |
| `arguments` | `ArgumentListNode` | Content |

| Member | Description |
|---|---|
| `isNullsafe(): bool` | Whether the call is written with ?->, which skips it when the object is null. |

### Expression\StaticMethodCallNode

Static method call: A::b(), $a::b(), A::{expr}().

Extends `ExpressionNode`.

| Slot | Type | Layout |
|---|---|---|
| `class` | `NameNode\|ExpressionNode` | Content |
| `doubleColon` | `Token` | Content |
| `openBrace` | `?Token` | Anchor |
| `name` | `IdentifierNode\|ExpressionNode` | Content |
| `closeBrace` | `?Token` | Closes |
| `arguments` | `ArgumentListNode` | Content |

### Expression\NewNode

Instantiation of a named, dynamic or anonymous class.

Extends `ExpressionNode`. Implements `OperatorNode`.

| Slot | Type | Layout |
|---|---|---|
| `newKeyword` | `Token` | Anchor |
| `class` | `NameNode\|ExpressionNode\|AnonymousClassNode` | Content |
| `arguments` | `?ArgumentListNode` | Content |

| Member | Description |
|---|---|
| `getPrecedence(): array` |  |

### Expression\ArrayNode

Array literal in either syntax: [...] or array(...). A short array standing where a place is assigned to destructures instead, and is a ListNode.

Extends `ExpressionNode`.

| Slot | Type | Layout |
|---|---|---|
| `arrayKeyword` | `?Token` | Anchor |
| `openDelimiter` | `Token` | Anchor |
| `items` | `SeparatedNodeList&lt;ArrayItemNode\|EmptyArrayItemNode>` | Content |
| `closeDelimiter` | `Token` | Closes |

### Expression\ListNode

Destructuring, written `list(...)` or `[...]`; the keyword is null for the short form. A short array is a literal until it stands where a place is assigned to, which is where it becomes one of these. It is no expression: it never carries a value.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `listKeyword` | `?Token` | Anchor |
| `openDelimiter` | `Token` | Anchor |
| `items` | `SeparatedNodeList&lt;ArrayItemNode\|EmptyArrayItemNode>` | Content |
| `closeDelimiter` | `Token` | Closes |

### Expression\AssignmentNode

Assignment `$a = $b`, whose operator is always `=`.

Extends `ExpressionNode`. Implements `OperatorNode`.

| Slot | Type | Layout |
|---|---|---|
| `target` | `ExpressionNode\|Expression\ListNode` | Content |
| `operator` | `Token` | Content |
| `expression` | `ExpressionNode` | Content |

| Member | Description |
|---|---|
| `getPrecedence(): array` |  |

### Expression\CombinedAssignmentNode

Combined assignment `$a += $b`, whose operator token tells which operation is baked into it.

Extends `ExpressionNode`. Implements `OperatorNode`.

| Slot | Type | Layout |
|---|---|---|
| `target` | `ExpressionNode` | Content |
| `operator` | `Token` | Content |
| `expression` | `ExpressionNode` | Content |

| Member | Description |
|---|---|
| `getPrecedence(): array` |  |

### Expression\AssignmentByReferenceNode

Assignment by reference `$a = &$b`; the grammar takes a variable or a new expression on the right.

Extends `ExpressionNode`. Implements `OperatorNode`.

| Slot | Type | Layout |
|---|---|---|
| `target` | `ExpressionNode` | Content |
| `equals` | `Token` | Content |
| `ampersand` | `Token` | Content |
| `expression` | `ExpressionNode` | Content |

| Member | Description |
|---|---|
| `getPrecedence(): array` |  |

### Expression\BinaryOpNode

Binary operation; the operator token tells which (arithmetic, comparison, logical, bitwise, concatenation, coalesce, pipe).

Extends `ExpressionNode`. Implements `OperatorNode`.

| Slot | Type | Layout |
|---|---|---|
| `left` | `ExpressionNode` | Content |
| `operator` | `Token` | Operator |
| `right` | `ExpressionNode` | Operator |

| Member | Description |
|---|---|
| `getPrecedence(): array` |  |

### Expression\UnaryOpNode

Unary operation written before its operand: +, -, !, ~, @; ++ and -- are a PrefixOpNode, the way the grammar tells them apart.

Extends `ExpressionNode`. Implements `OperatorNode`.

| Slot | Type | Layout |
|---|---|---|
| `operator` | `Token` | Content |
| `expression` | `ExpressionNode` | Content |

| Member | Description |
|---|---|
| `getPrecedence(): array` |  |

### Expression\PrefixOpNode

Prefix increment or decrement.

Extends `ExpressionNode`. Implements `OperatorNode`.

| Slot | Type | Layout |
|---|---|---|
| `operator` | `Token` | Content |
| `target` | `ExpressionNode` | Content |

| Member | Description |
|---|---|
| `getPrecedence(): array` |  |

### Expression\PostfixOpNode

Postfix increment or decrement.

Extends `ExpressionNode`. Implements `OperatorNode`.

| Slot | Type | Layout |
|---|---|---|
| `target` | `ExpressionNode` | Content |
| `operator` | `Token` | Content |

| Member | Description |
|---|---|
| `getPrecedence(): array` |  |

### Expression\CastNode

Type cast; the cast token keeps its spelling including inner whitespace: ( int ).

Extends `ExpressionNode`. Implements `OperatorNode`.

| Slot | Type | Layout |
|---|---|---|
| `cast` | `Token` | Content |
| `expression` | `ExpressionNode` | Content |

| Member | Description |
|---|---|
| `$type: string` | The type the cast converts to, in the name PHP knows it by: (integer) is int, (double) and (real) are float. |
| `getPrecedence(): array` |  |

### Expression\TernaryNode

Ternary conditional `$a ? $b : $c`, or the elvis form `$a ?: $c` leaving `then` empty.

Extends `ExpressionNode`. Implements `OperatorNode`.

| Slot | Type | Layout |
|---|---|---|
| `condition` | `ExpressionNode` | Content |
| `question` | `Token` | Branch |
| `then` | `?ExpressionNode` | Branch |
| `colon` | `Token` | Branch |
| `else` | `ExpressionNode` | Branch |

| Member | Description |
|---|---|
| `getPrecedence(): array` |  |

### Expression\InstanceofNode

instanceof check against a name or a dynamic class.

Extends `ExpressionNode`. Implements `OperatorNode`.

| Slot | Type | Layout |
|---|---|---|
| `expression` | `ExpressionNode` | Content |
| `instanceofKeyword` | `Token` | Operator |
| `class` | `NameNode\|ExpressionNode` | Operator |

| Member | Description |
|---|---|
| `getPrecedence(): array` |  |

### Expression\ParenthesizedNode

Expression in parentheses.

Extends `ExpressionNode`.

| Slot | Type | Layout |
|---|---|---|
| `openParen` | `Token` | Anchor |
| `expression` | `ExpressionNode` | Content |
| `closeParen` | `Token` | Closes |

| Member | Description |
|---|---|
| `isRedundant(): bool` | Whether the parentheses may go without the code coming to mean anything else: what stands in them binds at least as tightly as the place they stand in asks, and nothing reaches into them. Where the answer is not certain it is no. |

### Expression\IssetNode

isset(...) with one or more variables.

Extends `ExpressionNode`.

| Slot | Type | Layout |
|---|---|---|
| `issetKeyword` | `Token` | Anchor |
| `openParen` | `Token` | Anchor |
| `variables` | `SeparatedNodeList&lt;ExpressionNode>` | Content |
| `closeParen` | `Token` | Closes |

### Expression\EmptyNode

empty(...).

Extends `ExpressionNode`.

| Slot | Type | Layout |
|---|---|---|
| `emptyKeyword` | `Token` | Anchor |
| `openParen` | `Token` | Anchor |
| `expression` | `ExpressionNode` | Content |
| `closeParen` | `Token` | Closes |

### Expression\EvalNode

eval(...).

Extends `ExpressionNode`.

| Slot | Type | Layout |
|---|---|---|
| `evalKeyword` | `Token` | Anchor |
| `openParen` | `Token` | Anchor |
| `expression` | `ExpressionNode` | Content |
| `closeParen` | `Token` | Closes |

### Expression\IncludeNode

include, include_once, require or require_once; the keyword token tells which.

Extends `ExpressionNode`. Implements `RightExtendingNode`, `OperatorNode`.

| Slot | Type | Layout |
|---|---|---|
| `includeKeyword` | `Token` | Anchor |
| `expression` | `ExpressionNode` | Content |

| Member | Description |
|---|---|
| `getPrecedence(): array` |  |

### Expression\ExitNode

exit or die with optional arguments.

Extends `ExpressionNode`.

| Slot | Type | Layout |
|---|---|---|
| `exitKeyword` | `Token` | Anchor |
| `arguments` | `?ArgumentListNode` | Content |

### Expression\PrintNode

print expression.

Extends `ExpressionNode`. Implements `RightExtendingNode`, `OperatorNode`.

| Slot | Type | Layout |
|---|---|---|
| `printKeyword` | `Token` | Anchor |
| `expression` | `ExpressionNode` | Content |

| Member | Description |
|---|---|
| `getPrecedence(): array` |  |

### Expression\YieldNode

yield, yield $value or yield $key => $value.

Extends `ExpressionNode`. Implements `RightExtendingNode`, `OperatorNode`.

| Slot | Type | Layout |
|---|---|---|
| `yieldKeyword` | `Token` | Anchor |
| `key` | `?ExpressionNode` | Content |
| `doubleArrow` | `?Token` | Content |
| `value` | `?ExpressionNode` | Content |

| Member | Description |
|---|---|
| `getPrecedence(): array` |  |

### Expression\YieldFromNode

yield from expression.

Extends `ExpressionNode`. Implements `RightExtendingNode`, `OperatorNode`.

| Slot | Type | Layout |
|---|---|---|
| `yieldFromKeyword` | `Token` | Anchor |
| `expression` | `ExpressionNode` | Content |

| Member | Description |
|---|---|
| `getPrecedence(): array` |  |

### Expression\ThrowNode

throw expression.

Extends `ExpressionNode`. Implements `RightExtendingNode`, `OperatorNode`.

| Slot | Type | Layout |
|---|---|---|
| `throwKeyword` | `Token` | Anchor |
| `expression` | `ExpressionNode` | Content |

| Member | Description |
|---|---|
| `getPrecedence(): array` |  |

### Expression\CloneNode

clone expression; clone(...) with arguments is a function call.

Extends `ExpressionNode`. Implements `OperatorNode`.

| Slot | Type | Layout |
|---|---|---|
| `cloneKeyword` | `Token` | Anchor |
| `expression` | `ExpressionNode` | Content |

| Member | Description |
|---|---|
| `getPrecedence(): array` |  |

### Expression\MatchNode

match expression.

Extends `ExpressionNode`.

| Slot | Type | Layout |
|---|---|---|
| `matchKeyword` | `Token` | Anchor |
| `openParen` | `Token` | Anchor |
| `subject` | `ExpressionNode` | Content |
| `closeParen` | `Token` | Closes |
| `openBrace` | `Token` | Anchor |
| `arms` | `SeparatedNodeList&lt;MatchArmNode>` | Content |
| `closeBrace` | `Token` | Closes |

### Expression\ClosureNode

Anonymous function, optionally static, with captured variables.

Extends `ExpressionNode`. Implements `FunctionLikeNode`.

| Slot | Type | Layout |
|---|---|---|
| `attributes` | `NodeList&lt;AttributeGroupNode>` | Anchor |
| `staticKeyword` | `?Token` | Anchor |
| `functionKeyword` | `Token` | Anchor |
| `ampersand` | `?Token` | Content |
| `openParen` | `Token` | Anchor |
| `parameters` | `SeparatedNodeList&lt;ParameterNode>` | Content |
| `closeParen` | `Token` | Closes |
| `uses` | `?ClosureUsesNode` | Content |
| `colon` | `?Token` | Content |
| `returnType` | `?TypeNode` | Content |
| `body` | `Statement\BlockNode` | Body |

### Expression\ArrowFunctionNode

Arrow function fn(...) => expr, optionally static.

Extends `ExpressionNode`. Implements `FunctionLikeNode`, `RightExtendingNode`, `OperatorNode`.

| Slot | Type | Layout |
|---|---|---|
| `attributes` | `NodeList&lt;AttributeGroupNode>` | Anchor |
| `staticKeyword` | `?Token` | Anchor |
| `fnKeyword` | `Token` | Anchor |
| `ampersand` | `?Token` | Content |
| `openParen` | `Token` | Anchor |
| `parameters` | `SeparatedNodeList&lt;ParameterNode>` | Content |
| `closeParen` | `Token` | Closes |
| `colon` | `?Token` | Content |
| `returnType` | `?TypeNode` | Content |
| `doubleArrow` | `Token` | Content |
| `expression` | `ExpressionNode` | Content |

| Member | Description |
|---|---|
| `getPrecedence(): array` |  |

### Expression\ShellExecNode

Command in backticks with interpolation.

Extends `ExpressionNode`.

| Slot | Type | Layout |
|---|---|---|
| `openBacktick` | `Token` | Anchor |
| `parts` | `NodeList&lt;Scalar\InterpolatedStringPartNode\|Scalar\InterpolationNode\|ExpressionNode>` | Content |
| `closeBacktick` | `Token` | Closes |

### Scalar\IntegerNode

Integer literal in any base, kept as written.

Extends `ScalarNode`.

| Slot | Type | Layout |
|---|---|---|
| `token` | `Token` | Content |

| Member | Description |
|---|---|
| `$base: int` | The base the literal is written in: 2, 8, 10 or 16; a leading zero alone is the old octal notation. |
| `$value: int` | The value of the literal; a literal beyond the integer range is a float to PHP, so it is a FloatNode. |

### Scalar\FloatNode

Literal PHP reads as a float, kept as written. That is not the same as float syntax: an integer literal beyond the integer range is a float to PHP whatever its base (0xFFFFFFFFFFFFFFFF as much as 9223372036854775808), and the lexer hands it over as one, so such a literal is a node of this class.

Extends `ScalarNode`.

| Slot | Type | Layout |
|---|---|---|
| `token` | `Token` | Content |

| Member | Description |
|---|---|
| `$value: float` | The value of the literal, which is also how an integer literal beyond the integer range reaches PHP. |

### Scalar\BooleanNode

Boolean literal, kept in the letter case and with the leading backslash it is written with: true, FALSE, \True.

Extends `ScalarNode`.

| Slot | Type | Layout |
|---|---|---|
| `token` | `Token` | Content |

| Member | Description |
|---|---|
| `$value: bool` |  |

### Scalar\NullNode

Null literal, kept in the letter case and with the leading backslash it is written with: null, NULL, \Null.

Extends `ScalarNode`.

| Slot | Type | Layout |
|---|---|---|
| `token` | `Token` | Content |

### Scalar\StringNode

String literal without interpolation, quotes included.

Extends `ScalarNode`.

| Slot | Type | Layout |
|---|---|---|
| `token` | `Token` | Content |

| Member | Description |
|---|---|
| `$quote: string` | The delimiter the literal is written with: ' or ", a b or B prefix left out. |
| `$value: string` | The value of the literal with its escape sequences resolved. |
| `static fromValue(string $value, string $quote = '\''): Scalar\StringNode` | A literal standing for the value, escaped as the delimiter needs it. |
| `setValue(string $value, ?string $quote = null): void` | Writes the literal: the value escaped as the delimiter needs it, in the delimiter given or in the one it has. The two go together, because the delimiter decides how the value is written. |

### Scalar\UnquotedStringNode

Name of an offset written without quotes inside an interpolated string ("$row[label]"), which PHP reads as a string; having no delimiter, it stands for itself and holds no escape sequence.

Extends `ScalarNode`.

| Slot | Type | Layout |
|---|---|---|
| `token` | `Token` | Content |

| Member | Description |
|---|---|
| `$value: string` | The value of the offset, which is the text as it stands. |

### Scalar\MagicConstantNode

Magic constant: __LINE__, __FILE__, __DIR__, __CLASS__, __TRAIT__, __METHOD__, __FUNCTION__, __PROPERTY__, __NAMESPACE__.

Extends `ScalarNode`.

| Slot | Type | Layout |
|---|---|---|
| `token` | `Token` | Content |

### Scalar\InterpolatedStringNode

Double-quoted string with interpolated variables or expressions.

Extends `ScalarNode`.

| Slot | Type | Layout |
|---|---|---|
| `openQuote` | `Token` | Anchor |
| `parts` | `NodeList&lt;Scalar\InterpolatedStringPartNode\|Scalar\InterpolationNode\|ExpressionNode>` | Content |
| `closeQuote` | `Token` | Closes |

### Scalar\HeredocNode

Heredoc or nowdoc; the closing delimiter keeps its indentation, the parts keep theirs.

Extends `ScalarNode`.

| Slot | Type | Layout |
|---|---|---|
| `openDelimiter` | `Token` | Anchor |
| `parts` | `NodeList&lt;Scalar\InterpolatedStringPartNode\|Scalar\InterpolationNode\|ExpressionNode>` | Content |
| `closeDelimiter` | `Token` | Closes |

| Member | Description |
|---|---|
| `$label: string` | The label between &lt;&lt;&lt; and the body, without quotes. |
| `$indentation: string` | The indentation of the closing delimiter, which the body shares. |
| `$value: string` | The text of the body with its escape sequences resolved and the common indentation removed. |
| `isNowdoc(): bool` | A nowdoc has its label in single quotes and resolves no escape sequences. |
| `hasInterpolation(): bool` | Whether any part of the body is an interpolation, which leaves the heredoc without a value of its own. |

### Scalar\InterpolatedStringPartNode

Literal text between interpolations, whitespace included. What its escape sequences mean depends on the string it stands in, so the string is what resolves them, with PhpSyntax\Escaping.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `token` | `Token` | Content |

### Scalar\InterpolationNode

Braced interpolation inside a string: {$expr} or ${name}.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `openBrace` | `Token` | Anchor |
| `expression` | `ExpressionNode` | Content |
| `closeBrace` | `Token` | Closes |

### Type\NamedTypeNode

Type given by a name: builtin (int, static, array, callable) or a class.

Extends `TypeNode`.

| Slot | Type | Layout |
|---|---|---|
| `name` | `NameNode` | Content |

| Member | Description |
|---|---|
| `isBuiltin(): bool` | Whether the type is one PHP knows itself, self, static and parent among them. |

### Type\NullableTypeNode

Nullable type: ?T.

Extends `TypeNode`.

| Slot | Type | Layout |
|---|---|---|
| `question` | `Token` | Content |
| `type` | `TypeNode` | Content |

### Type\UnionTypeNode

Union type A|B; a member may be a parenthesized intersection (DNF).

Extends `TypeNode`.

| Slot | Type | Layout |
|---|---|---|
| `types` | `SeparatedNodeList&lt;TypeNode>` | Anchor |

### Type\IntersectionTypeNode

Intersection type A&B, parenthesized inside a union.

Extends `TypeNode`.

| Slot | Type | Layout |
|---|---|---|
| `openParen` | `?Token` | Anchor |
| `types` | `SeparatedNodeList&lt;TypeNode>` | Anchor |
| `closeParen` | `?Token` | Closes |

### Statement\NamespaceNode

namespace declaration; after "namespace A;" the following statements are nested in it.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `namespaceKeyword` | `Token` | Anchor |
| `name` | `?NameNode` | Content |
| `semicolon` | `?Token` | Anchor |
| `openBrace` | `?Token` | Anchor |
| `statements` | `NodeList&lt;StatementNode>` | Content |
| `closeBrace` | `?Token` | Closes |

### Statement\UseNode

use import of classes, functions or constants, written item by item or as a group under a prefix (use A\{B, C};), which fills the slots the other form leaves empty.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `useKeyword` | `Token` | Anchor |
| `type` | `?Token` | Content |
| `prefix` | `?NameNode` | Content |
| `namespaceSeparator` | `?Token` | Content |
| `openBrace` | `?Token` | Anchor |
| `items` | `SeparatedNodeList&lt;UseItemNode>` | Content |
| `closeBrace` | `?Token` | Closes |
| `semicolon` | `Token` | Anchor |

| Member | Description |
|---|---|
| `$kind: SymbolKind` | What the statement imports, which every item without a type of its own imports too. |
| `isGroup(): bool` | Whether the items are written as a group under a prefix, which every one of them imports. |
| `addImport(string $name, ?string $alias = null, ?int $index = null): UseItemNode` | Adds an import of the fully qualified name, written the way the statement writes its items: whole in a plain import, under the prefix in a group, which refuses a name standing outside it. The item imports what the statement imports and goes last unless an index says where. |

### Statement\ConstNode

const statement outside a class.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `attributes` | `NodeList&lt;AttributeGroupNode>` | Anchor |
| `constKeyword` | `Token` | Anchor |
| `items` | `SeparatedNodeList&lt;ConstItemNode>` | Content |
| `semicolon` | `Token` | Anchor |

### Statement\HaltCompilerNode

__halt_compiler(); the rest of the file is the data token.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `haltKeyword` | `Token` | Anchor |
| `openParen` | `Token` | Anchor |
| `closeParen` | `Token` | Closes |
| `semicolon` | `Token` | Anchor |
| `data` | `?Token` | Content |

### Statement\InlineHtmlNode

Text outside PHP tags, including a BOM or a hashbang line.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `html` | `Token` | Content |

| Member | Description |
|---|---|
| `isPreamble(): bool` | Whether the text is only what may precede the code of a pure PHP file: a byte order mark, a hashbang line, or both. |

### Statement\EmptyStatementNode

Bare semicolon, or a close tag after a terminated statement.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `semicolon` | `Token` | Anchor |

### Statement\ExpressionStatementNode

Expression as a statement.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `expression` | `ExpressionNode` | Content |
| `semicolon` | `Token` | Anchor |

### Statement\BlockNode

Statements in braces.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `openBrace` | `Token` | Anchor |
| `statements` | `NodeList&lt;StatementNode>` | Content |
| `closeBrace` | `Token` | Closes |

### Statement\IfNode

if statement in either syntax; the body is a statement, the alternative syntax fills statements.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `ifKeyword` | `Token` | Anchor |
| `openParen` | `Token` | Anchor |
| `condition` | `ExpressionNode` | Content |
| `closeParen` | `Token` | Closes |
| `body` | `?StatementNode` | Body |
| `colon` | `?Token` | Content |
| `statements` | `?NodeList&lt;StatementNode>` | Content |
| `elseifs` | `NodeList&lt;ElseIfNode>` | Anchor |
| `else` | `?ElseNode` | Anchor |
| `endKeyword` | `?Token` | Closes |
| `semicolon` | `?Token` | Anchor |

### Statement\WhileNode

while loop in either syntax.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `whileKeyword` | `Token` | Anchor |
| `openParen` | `Token` | Anchor |
| `condition` | `ExpressionNode` | Content |
| `closeParen` | `Token` | Closes |
| `body` | `?StatementNode` | Body |
| `colon` | `?Token` | Content |
| `statements` | `?NodeList&lt;StatementNode>` | Content |
| `endKeyword` | `?Token` | Closes |
| `semicolon` | `?Token` | Anchor |

### Statement\DoWhileNode

do-while loop.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `doKeyword` | `Token` | Anchor |
| `body` | `StatementNode` | Body |
| `whileKeyword` | `Token` | Anchor |
| `openParen` | `Token` | Anchor |
| `condition` | `ExpressionNode` | Content |
| `closeParen` | `Token` | Closes |
| `semicolon` | `Token` | Anchor |

### Statement\ForNode

for loop in either syntax.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `forKeyword` | `Token` | Anchor |
| `openParen` | `Token` | Anchor |
| `initializers` | `SeparatedNodeList&lt;ExpressionNode>` | Content |
| `firstSemicolon` | `Token` | Content |
| `conditions` | `SeparatedNodeList&lt;ExpressionNode>` | Content |
| `secondSemicolon` | `Token` | Content |
| `updates` | `SeparatedNodeList&lt;ExpressionNode>` | Content |
| `closeParen` | `Token` | Closes |
| `body` | `?StatementNode` | Body |
| `colon` | `?Token` | Content |
| `statements` | `?NodeList&lt;StatementNode>` | Content |
| `endKeyword` | `?Token` | Closes |
| `semicolon` | `?Token` | Anchor |

### Statement\ForeachNode

foreach loop in either syntax.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `foreachKeyword` | `Token` | Anchor |
| `openParen` | `Token` | Anchor |
| `expression` | `ExpressionNode` | Content |
| `asKeyword` | `Token` | Content |
| `key` | `?ExpressionNode` | Content |
| `doubleArrow` | `?Token` | Content |
| `ampersand` | `?Token` | Content |
| `value` | `ExpressionNode\|Expression\ListNode` | Content |
| `closeParen` | `Token` | Closes |
| `body` | `?StatementNode` | Body |
| `colon` | `?Token` | Content |
| `statements` | `?NodeList&lt;StatementNode>` | Content |
| `endKeyword` | `?Token` | Closes |
| `semicolon` | `?Token` | Anchor |

### Statement\SwitchNode

switch statement in either syntax; a semicolon may precede the first case.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `switchKeyword` | `Token` | Anchor |
| `openParen` | `Token` | Anchor |
| `subject` | `ExpressionNode` | Content |
| `closeParen` | `Token` | Closes |
| `openBrace` | `?Token` | Anchor |
| `colon` | `?Token` | Content |
| `leadingSemicolon` | `?Token` | Content |
| `cases` | `NodeList&lt;CaseNode>` | Case |
| `closeBrace` | `?Token` | Closes |
| `endKeyword` | `?Token` | Closes |
| `semicolon` | `?Token` | Anchor |

### Statement\BreakNode

break with an optional level.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `breakKeyword` | `Token` | Anchor |
| `expression` | `?ExpressionNode` | Content |
| `semicolon` | `Token` | Anchor |

### Statement\ContinueNode

continue with an optional level.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `continueKeyword` | `Token` | Anchor |
| `expression` | `?ExpressionNode` | Content |
| `semicolon` | `Token` | Anchor |

### Statement\ReturnNode

return with an optional value.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `returnKeyword` | `Token` | Anchor |
| `expression` | `?ExpressionNode` | Content |
| `semicolon` | `Token` | Anchor |

### Statement\GlobalNode

global statement.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `globalKeyword` | `Token` | Anchor |
| `variables` | `SeparatedNodeList&lt;ExpressionNode>` | Content |
| `semicolon` | `Token` | Anchor |

### Statement\StaticNode

static variable declaration.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `staticKeyword` | `Token` | Anchor |
| `variables` | `SeparatedNodeList&lt;StaticVariableNode>` | Content |
| `semicolon` | `Token` | Anchor |

### Statement\EchoNode

echo statement; the keyword may be the <?= open tag and the semicolon a close tag.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `echoKeyword` | `Token` | Anchor |
| `expressions` | `SeparatedNodeList&lt;ExpressionNode>` | Content |
| `semicolon` | `Token` | Anchor |

### Statement\UnsetNode

unset statement.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `unsetKeyword` | `Token` | Anchor |
| `openParen` | `Token` | Anchor |
| `variables` | `SeparatedNodeList&lt;ExpressionNode>` | Content |
| `closeParen` | `Token` | Closes |
| `semicolon` | `Token` | Anchor |

### Statement\DeclareNode

declare statement in its three forms: with a body, with a bare semicolon, or with the alternative syntax.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `declareKeyword` | `Token` | Anchor |
| `openParen` | `Token` | Anchor |
| `items` | `SeparatedNodeList&lt;DeclareItemNode>` | Content |
| `closeParen` | `Token` | Closes |
| `body` | `?StatementNode` | Body |
| `colon` | `?Token` | Content |
| `statements` | `?NodeList&lt;StatementNode>` | Content |
| `endKeyword` | `?Token` | Closes |
| `semicolon` | `?Token` | Anchor |

### Statement\TryNode

try statement with catches and an optional finally.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `tryKeyword` | `Token` | Anchor |
| `body` | `Statement\BlockNode` | Body |
| `catches` | `NodeList&lt;CatchNode>` | Anchor |
| `finally` | `?FinallyNode` | Anchor |

### Statement\GotoNode

goto statement.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `gotoKeyword` | `Token` | Anchor |
| `label` | `IdentifierNode` | Content |
| `semicolon` | `Token` | Anchor |

### Statement\LabelNode

Label for goto.

Extends `StatementNode`.

| Slot | Type | Layout |
|---|---|---|
| `name` | `IdentifierNode` | Content |
| `colon` | `Token` | Content |

### Statement\FunctionNode

Function declaration.

Extends `StatementNode`. Implements `FunctionLikeNode`.

| Slot | Type | Layout |
|---|---|---|
| `attributes` | `NodeList&lt;AttributeGroupNode>` | Anchor |
| `functionKeyword` | `Token` | Anchor |
| `ampersand` | `?Token` | Content |
| `name` | `IdentifierNode` | Content |
| `openParen` | `Token` | Anchor |
| `parameters` | `SeparatedNodeList&lt;ParameterNode>` | Content |
| `closeParen` | `Token` | Closes |
| `colon` | `?Token` | Content |
| `returnType` | `?TypeNode` | Content |
| `body` | `Statement\BlockNode` | Body |

### Statement\ClassNode

Class declaration.

Extends `StatementNode`. Implements `ClassLikeNode`.

| Slot | Type | Layout |
|---|---|---|
| `attributes` | `NodeList&lt;AttributeGroupNode>` | Anchor |
| `modifiers` | `ModifiersNode` | Anchor |
| `classKeyword` | `Token` | Anchor |
| `name` | `IdentifierNode` | Content |
| `extendsKeyword` | `?Token` | Content |
| `extends` | `?NameNode` | Content |
| `implementsKeyword` | `?Token` | Content |
| `implements` | `?SeparatedNodeList&lt;NameNode>` | Content |
| `openBrace` | `Token` | Anchor |
| `members` | `NodeList&lt;MemberNode>` | Content |
| `closeBrace` | `Token` | Closes |

### Statement\InterfaceNode

Interface declaration.

Extends `StatementNode`. Implements `ClassLikeNode`.

| Slot | Type | Layout |
|---|---|---|
| `attributes` | `NodeList&lt;AttributeGroupNode>` | Anchor |
| `interfaceKeyword` | `Token` | Anchor |
| `name` | `IdentifierNode` | Content |
| `extendsKeyword` | `?Token` | Content |
| `extends` | `?SeparatedNodeList&lt;NameNode>` | Content |
| `openBrace` | `Token` | Anchor |
| `members` | `NodeList&lt;MemberNode>` | Content |
| `closeBrace` | `Token` | Closes |

### Statement\TraitNode

Trait declaration.

Extends `StatementNode`. Implements `ClassLikeNode`.

| Slot | Type | Layout |
|---|---|---|
| `attributes` | `NodeList&lt;AttributeGroupNode>` | Anchor |
| `traitKeyword` | `Token` | Anchor |
| `name` | `IdentifierNode` | Content |
| `openBrace` | `Token` | Anchor |
| `members` | `NodeList&lt;MemberNode>` | Content |
| `closeBrace` | `Token` | Closes |

### Statement\EnumNode

Enum declaration, optionally backed by a scalar type.

Extends `StatementNode`. Implements `ClassLikeNode`.

| Slot | Type | Layout |
|---|---|---|
| `attributes` | `NodeList&lt;AttributeGroupNode>` | Anchor |
| `enumKeyword` | `Token` | Anchor |
| `name` | `IdentifierNode` | Content |
| `colon` | `?Token` | Content |
| `scalarType` | `?TypeNode` | Content |
| `implementsKeyword` | `?Token` | Content |
| `implements` | `?SeparatedNodeList&lt;NameNode>` | Content |
| `openBrace` | `Token` | Anchor |
| `members` | `NodeList&lt;MemberNode>` | Content |
| `closeBrace` | `Token` | Closes |

### Member\PropertyNode

Property declaration; one or more properties, optionally with hooks.

Extends `MemberNode`.

| Slot | Type | Layout |
|---|---|---|
| `attributes` | `NodeList&lt;AttributeGroupNode>` | Anchor |
| `modifiers` | `ModifiersNode` | Anchor |
| `type` | `?TypeNode` | Anchor |
| `items` | `SeparatedNodeList&lt;Member\PropertyItemNode>` | Content |
| `semicolon` | `?Token` | Anchor |
| `openBrace` | `?Token` | Anchor |
| `hooks` | `?NodeList&lt;Member\PropertyHookNode>` | Content |
| `closeBrace` | `?Token` | Closes |

### Member\PropertyItemNode

One property of a declaration with an optional default.

Extends `Node`.

| Slot | Type | Layout |
|---|---|---|
| `name` | `Token` | Content |
| `equals` | `?Token` | Content |
| `default` | `?ExpressionNode` | Content |

| Member | Description |
|---|---|
| `$plainName: string` | The name of the property without the dollar sign. |

### Member\PropertyHookNode

Property hook (get, set) with a block body, an arrow body or none.

Extends `Node`. Implements `FunctionLikeNode`.

| Slot | Type | Layout |
|---|---|---|
| `attributes` | `NodeList&lt;AttributeGroupNode>` | Anchor |
| `modifiers` | `ModifiersNode` | Anchor |
| `ampersand` | `?Token` | Anchor |
| `name` | `IdentifierNode` | Anchor |
| `openParen` | `?Token` | Anchor |
| `parameters` | `?SeparatedNodeList&lt;ParameterNode>` | Content |
| `closeParen` | `?Token` | Closes |
| `body` | `?Statement\BlockNode` | Body |
| `doubleArrow` | `?Token` | Content |
| `expression` | `?ExpressionNode` | Content |
| `semicolon` | `?Token` | Anchor |

| Member | Description |
|---|---|
| `$returnType: ?TypeNode` |  |

### Member\ClassConstNode

Class constant declaration, optionally typed.

Extends `MemberNode`.

| Slot | Type | Layout |
|---|---|---|
| `attributes` | `NodeList&lt;AttributeGroupNode>` | Anchor |
| `modifiers` | `ModifiersNode` | Anchor |
| `constKeyword` | `Token` | Anchor |
| `type` | `?TypeNode` | Anchor |
| `items` | `SeparatedNodeList&lt;ConstItemNode>` | Content |
| `semicolon` | `Token` | Anchor |

### Member\MethodNode

Method declaration; abstract and interface methods end with a semicolon instead of a body.

Extends `MemberNode`. Implements `FunctionLikeNode`.

| Slot | Type | Layout |
|---|---|---|
| `attributes` | `NodeList&lt;AttributeGroupNode>` | Anchor |
| `modifiers` | `ModifiersNode` | Anchor |
| `functionKeyword` | `Token` | Anchor |
| `ampersand` | `?Token` | Content |
| `name` | `IdentifierNode` | Content |
| `openParen` | `Token` | Anchor |
| `parameters` | `SeparatedNodeList&lt;ParameterNode>` | Content |
| `closeParen` | `Token` | Closes |
| `colon` | `?Token` | Content |
| `returnType` | `?TypeNode` | Content |
| `body` | `?Statement\BlockNode` | Body |
| `semicolon` | `?Token` | Anchor |

| Member | Description |
|---|---|
| `isConstructor(): bool` | Whether the method is the constructor, whose name PHP compares without regard to letter case. |

### Member\TraitUseNode

use of traits with optional adaptations in braces.

Extends `MemberNode`.

| Slot | Type | Layout |
|---|---|---|
| `useKeyword` | `Token` | Anchor |
| `traits` | `SeparatedNodeList&lt;NameNode>` | Content |
| `semicolon` | `?Token` | Anchor |
| `openBrace` | `?Token` | Anchor |
| `adaptations` | `?NodeList&lt;Member\TraitAdaptationNode>` | Content |
| `closeBrace` | `?Token` | Closes |

### Member\TraitPrecedenceNode

Trait adaptation A::m insteadof B;.

Extends `Member\TraitAdaptationNode`.

| Slot | Type | Layout |
|---|---|---|
| `trait` | `NameNode` | Content |
| `doubleColon` | `Token` | Content |
| `method` | `IdentifierNode` | Content |
| `insteadofKeyword` | `Token` | Content |
| `traits` | `SeparatedNodeList&lt;NameNode>` | Content |
| `semicolon` | `Token` | Anchor |

### Member\TraitAliasNode

Trait adaptation m as [modifier] [alias];.

Extends `Member\TraitAdaptationNode`.

| Slot | Type | Layout |
|---|---|---|
| `trait` | `?NameNode` | Content |
| `doubleColon` | `?Token` | Content |
| `method` | `IdentifierNode` | Content |
| `asKeyword` | `Token` | Content |
| `modifier` | `?Token` | Content |
| `alias` | `?IdentifierNode` | Content |
| `semicolon` | `Token` | Anchor |

### Member\EnumCaseNode

Enum case with an optional backing value.

Extends `MemberNode`.

| Slot | Type | Layout |
|---|---|---|
| `attributes` | `NodeList&lt;AttributeGroupNode>` | Anchor |
| `caseKeyword` | `Token` | Anchor |
| `name` | `IdentifierNode` | Content |
| `equals` | `?Token` | Content |
| `value` | `?ExpressionNode` | Content |
| `semicolon` | `Token` | Anchor |

